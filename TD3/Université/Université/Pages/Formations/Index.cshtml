@page
@model Université.Pages.Formations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].Intitule)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].AnneDiplome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].NbInscrits)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].NbUE)
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Formation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Intitule)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnneDiplome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NbInscrits)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NbUE)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
